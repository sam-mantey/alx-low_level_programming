#include <stdio.h>

/**
 * main - Prints the name of the programme
 * @argc: Count arguments to main
 * @argv: a char array of the agruments to main
 */

int main(int argc, char *argv[])
{

	printf("%s\n", argv[0]);
	return (0);
}
