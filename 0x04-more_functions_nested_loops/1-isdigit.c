#include "main.h"

/**
 * _isdigit - Checks if an input is a digit
 *
 * Return 1 (if its true) and 0 (when false)
 */
int _isdigit(int c)
{
	if (c >= 0 && c < 10)
	{
	return (1);
	}
	else
	{
	return (0);
	}
}
