#include "main.h"

/**
 * _islower - Checks if an input is in lowercase
 *
 * Return: Always 0
 */
int _islower(int c)
{
	int c;

	if (c > 'a' && c <= 'z')
	{
	return (1);
	}
	else 
	{
	return (0);
	}
}
