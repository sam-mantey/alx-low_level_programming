#include "main.h"

/**
 * print_sign - Prints the sigh of an integer
 * @n: Contains integer to be compared
 * Return: always 0
 */
int print_sign(int n)
{
	if (n > 0)
	{
	_putchar('+');
	}
	else if (n == 0 )
	{
	_putchar('0');
	}
	else if
	{
	_putchar('-');
	}
	_putchar('\n');
}
