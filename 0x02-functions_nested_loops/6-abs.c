#include "main.h"

/**
 * _abs - this returns the absolute value of an input
 *
 * Return: Always 0
 */
int _abs(int)
{
	if (n < 0)
	{
	return (n * (-1));
	}
	else if (n == o)
	{
	return (0);
	}
	else
	{
	return (n);
	}
}
