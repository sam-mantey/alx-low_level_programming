#include "main.h"

/**
 * main - Check the code
 *
 * Return: Always zero
 */
void print_alphabet(void)
{
	char alph;
	for (alph = 'a'; alph <= 'z'; alph++)
	{
		_putchar(alph);
	}
	_putchar('\n');
}
